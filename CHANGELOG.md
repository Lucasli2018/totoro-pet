# Changelog

## 0.1.2 (2026-08-25)
- **修复**：关闭插件再打开不显示 —— `apply()` 改为直接调用 `ctx.slots.inject(...)`（与官方 `dsh-pocket` 同构），移除误用的 `ctx.effect` 包装。原写法回调未返回 disposer 且携带多余 label，导致插件禁用/卸载时 `shell.overlay` 注册残留、指向失效 `ctx`。
- **修复**：点击宠物/恢复后不显示 —— `stageNode` 改为渲染时直接注入当前姿态 SVG（`dangerouslySetInnerHTML`），不再依赖 `pose` 变化的 `useEffect`（从隐藏态恢复时 `pose` 不变、effect 不触发，宠物区域为空）。

## 0.1.1 (2026-08-24)
- 宿主半边冒烟测试 18 项全绿。
- 完善三维养成数值曲线与五类口吻台词库。

## 0.1.0 (2026-08-20)
- 首发：悬浮 Q 版龙猫桌宠，常驻页面右下角。
- 四姿态 SVG 动画（待机/开心/睡觉/进食），切换不跳形。
- 四种互动（喂食/抚摸/玩耍/睡觉·唤醒），含冷却与体力门槛小脾气。
- 三维养成（饱食/心情/精力）+ 经验等级；离线无缝降级本地台词。
- 拖拽换位、外观自定义、闲置自动隐藏、一键停用（缩成小圆点）。
- 图鉴弹卡、回环 HTTP API、原子持久化，零构建纯 JS 双半边架构。
